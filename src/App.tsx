import './App.css'
import Header from './components/Header'

function App() {

  const totalCount = 0;
  const totalPrice = 0;
  return (
    <div>
      <Header totalCount={totalCount} totalPrice={totalPrice} />
    </div>
  )
}

export default App
